{
  "name": "startup-markup",
  "version": "1.0.0",
  "description": "Start up landing page markup training project",
  "main": "index.html",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./css/main.css",
    "watch": "tailwindcss -i ./input.css -o ./css/main.css --watch"
  },
  "keywords": ["html", "css", "tailwind"],
  "devDependencies": {
    "tailwindcss": "^3.2.2"
  }
}
